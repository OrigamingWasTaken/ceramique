/**
 * Loads a YAML file in the set config directory (default: config) based on the provided name.
 *
 * @param {string} fileName - The name of the YAML file to load.
 * @returns {any} The parsed YAML content.
 */
declare function loadYaml(fileName: string): any;